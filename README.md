# Loyalty Points Wallet

## Quick Start
1. Copy `.env.example` to `.env`.
2. Run `docker-compose up --build`.
3. In a new terminal, run migrations: `docker-compose exec api python manage.py migrate`.
4. Access the API at `http://localhost:8000`.
